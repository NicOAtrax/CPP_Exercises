#include <iostream>

int main()
{
    using namespace std;
    while (true)
    {
        cout << "This is a translator, but it does nothing for now..." << std::endl;
        cin.ignore();
    }

    return 0;
}